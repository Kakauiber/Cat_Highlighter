# 划线猫隐私说明

最后更新：2026-05-20

划线猫是一款浏览器本地标注与笔记工具。它用于帮助用户在网页和大模型对话页面中保存高亮、划线、批注、页面笔记和页面标签，并在用户主动操作时导出到指定工具。

## 我们收集和保存哪些数据

划线猫会在浏览器本地保存以下数据：

- 页面标题和页面 URL。
- 用户主动创建的高亮、划线记录。
- 高亮颜色、划线类型、创建时间等用于恢复标注的必要信息。
- 用户为高亮添加的批注。
- 用户在侧边栏写下的页面笔记。
- 用户为页面或 AI 对话添加的页面标签。
- 用户在管理页设置的导出配置，例如墨问 API Key、Notion API 集成密钥、Notion 目标父页面 ID、Obsidian 仓库名称、思源 API Token、思源服务地址、目标笔记本和目录。
- 用户设置的黑名单网站等插件配置。

划线猫不会主动收集用户完整浏览历史。只有用户在某个页面创建高亮、划线或笔记后，该页面的标题和 URL 才会被保存，用于后续恢复和管理。

## 数据保存在哪里

默认情况下，划线猫的数据保存在浏览器本地的 `chrome.storage.local` 中。

划线猫目前没有自建服务器，不会把用户数据上传到划线猫服务器。

如果用户卸载插件，浏览器通常会清除该插件对应的本地扩展数据。用户也可以在侧边栏或管理页中删除单条记录、本页记录、页面笔记或批量删除记录。

## 什么时候会向第三方发送数据

只有当用户主动点击导出功能时，划线猫才会把相关内容发送到用户选择的第三方服务。

### 墨问

当用户主动导出到墨问时，划线猫会把选定页面的标题、链接、页面笔记、高亮、划线和批注内容发送到墨问 Open API，并使用用户填写的墨问 API Key 创建私密笔记。

### Notion

当用户主动导出到 Notion 时，划线猫会把选定页面的标题、链接、页面笔记、高亮、划线和批注内容发送到 Notion API，并使用用户填写的 Notion API 集成密钥在目标父页面下创建子页面。

### Obsidian

当用户主动导出到 Obsidian 时，划线猫会把导出内容写入剪贴板，并通过 `obsidian://new` 协议请求 Obsidian 桌面端创建笔记。内容不会发送到划线猫服务器。

### 思源笔记

当用户主动导出到思源笔记时，划线猫会把导出内容发送到用户配置的思源本地服务地址，通常是 `http://127.0.0.1:6806`。这用于在本机思源桌面版中创建文档。

### Markdown / HTML

当用户导出 Markdown 或 HTML 文件时，划线猫只会在本地生成并下载文件，不会把内容发送到第三方服务。

## API Key 和 Token 如何处理

用户填写的墨问 API Key、Notion API 集成密钥、思源 API Token 等配置保存在浏览器本地 `chrome.storage.local` 中。

这些密钥只会在用户主动使用对应导出功能时发送给对应服务：

- 墨问 API Key 只发送给墨问 Open API。
- Notion API 集成密钥只发送给 Notion API。
- 思源 API Token 只发送给用户配置的思源服务地址。

划线猫不会把这些密钥上传到自建服务器，也不会出售、出租或共享这些密钥。

## 浏览器权限说明

划线猫使用以下浏览器权限：

- `storage`：保存高亮、批注、页面笔记、页面标签和导出配置。
- `activeTab`：识别当前浏览器标签页，以便侧边栏显示当前页面记录。
- `scripting`：在网页中执行必要脚本，用于创建和恢复高亮。
- `clipboardWrite`：在用户点击复制或导出到 Obsidian 时写入剪贴板。
- `sidePanel`：提供浏览器侧边栏界面。
- `<all_urls>`：允许用户在任意网页上选择文字并创建高亮。插件只会在用户使用功能时保存相关页面数据。
- `all_frames`：支持复杂网页和嵌入式框架页面中的选区识别与高亮恢复。
- `match_about_blank` / `match_origin_as_fallback`：支持部分复杂网页、嵌入式页面和特殊框架场景中的选区识别。

## 远程代码

划线猫不加载或执行远程托管代码。插件代码随扩展包一同发布。

划线猫会在用户主动导出时向第三方 API 发起网络请求，但不会从这些服务下载并执行远程脚本。

## 用户如何删除数据

用户可以通过以下方式删除数据：

- 在侧边栏删除单条高亮。
- 在侧边栏删除当前页面高亮。
- 在侧边栏清空当前页面笔记。
- 在侧边栏或管理页移除页面标签。
- 在管理页删除单条记录、页面记录或批量删除记录。
- 在浏览器扩展管理页卸载划线猫。
- 在浏览器设置中清理扩展本地数据。

## 数据使用承诺

划线猫仅为了提供网页标注、页面笔记、记录恢复和用户主动导出功能而处理用户数据。

划线猫不会：

- 出售用户数据。
- 将用户数据用于广告定向。
- 将用户数据用于信用评估或身份画像。
- 在未获得用户主动操作的情况下，把用户网页内容上传到第三方服务。

## 联系方式

如果你对隐私说明或数据处理方式有疑问，请通过项目仓库或 Chrome Web Store 支持渠道联系开发者。
